{
  "name": "Static",
  "version": "1.0",
  "description": "Static",
  "author": "Your Name",
  "license": "ISC",
  "scripts": {
    "build": "npm run build:css && npm run build:images && node scripts/sync-public-assets.js && npx @devdojo/static build && node scripts/generate-post-pagination.js && node scripts/generate-sitemap.js && node scripts/generate-news-sitemap.js && node scripts/generate-sitemap-index.js && node scripts/generate-rss.js && node scripts/generate-json-feed.js && node scripts/post-build-seo.js && node scripts/check-internal-links.js",
    "build:css": "npx @tailwindcss/cli -i ./styles/app.css -o ./public/styles.css --minify",
    "build:images": "node scripts/generate-webp.js",
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.2",
    "@tailwindcss/typography": "^0.5.10",
    "sharp": "^0.34.2"
  }
}
