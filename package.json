{
    "name": "Static",
    "version": "1.0",
    "description": "Static",
    "author": "Your Name",
    "license": "ISC",
    "scripts": {
        "build": "npx @devdojo/static build && node scripts/generate-post-pagination.js && node scripts/generate-sitemap.js && node scripts/generate-rss.js && node scripts/post-build-seo.js"
    },
    "devDependencies": {
        "@tailwindcss/typography": "^0.5.10"
    }
}
